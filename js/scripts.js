var convert = function(number1, number2) {
  return number1 * number2;
};

var number1 = parseInt(prompt("Enter the amount of liters:"));
var number2 = parseFloat(3.785);

alert(convert(number1, number2));
